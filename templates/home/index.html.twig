{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <div class="p-3 border rounded">
        <h1 class="text-center mb-5">Bienvenue sur StoryDrawer</h1>
        <p class="font-weight-bold text-center">Dessinez votre histoire ou laissez votre histoire vous dessiner</p>
        {# ========= user NOT connected ========= #}
        {% if app.user == "" %}
            {# <div class="text-center mt-5">
                <a class="btn btn-primary m-3 col-11 col-sm-3" href="{{ path("app_login") }}">
                    <h3>Se connecter</h3>
                </a>
            </div> #}
            <div class="mt-3">
                <h3 class="text-center">
                    Connectez-vous pour accéder aux histoires.
                </h3>
                <h3 class="mt-5 mb-4 text-center">Déjà un compte?</h3>
                <a href=" {{ path('app_login')}} " class="btn button button-primary mb-3 col-12">Connectez vous</a>

            </div>
        {# =========== user connected =========== #}
        {% else %}
        <div class="m-3 text-center">
                <a class="btn button button-success" href="{{ path("story_index") }}" style="margin-top: 10%">
                    <h2>Accéder aux histoires</h2>
                </a>
            </div>
        {% endif %}
    </div>
{% endblock %}
